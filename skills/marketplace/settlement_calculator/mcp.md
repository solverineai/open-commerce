# Future MCP Support

Expose `settlement_calculator` as `opencommerce.settlement_calculator`.

The MCP tool should return timing confidence separately from amount confidence because settlement schedules often depend on provider policy and account status.
