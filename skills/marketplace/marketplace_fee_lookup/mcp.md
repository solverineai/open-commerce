# Future MCP Support

Expose `marketplace_fee_lookup` as `opencommerce.marketplace_fee_lookup`.

The MCP tool should include dataset version, source references, and confidence warnings in every response.
