# Future MCP Support

Expose `fulfillment_recommendation` as `opencommerce.fulfillment_recommendation`.

The MCP tool should be advisory and should require explicit downstream authorization before any shipment action is executed.
