# Future MCP Support

Expose `demand_forecast` as `opencommerce.demand_forecast`.

The MCP tool should return `forecast.schema.json` compatible output and include confidence notes when historical data is sparse.
