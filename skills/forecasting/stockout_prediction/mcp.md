# Future MCP Support

Expose `stockout_prediction` as `opencommerce.stockout_prediction`.

The MCP tool should accept inventory and forecast records and return risk fields that can be used by reorder workflows.
