# Future MCP Support

Expose `break_even_analysis` as `opencommerce.break_even_analysis`.

The MCP tool should identify whether fees are absolute costs or derived rates. Dataset-derived rates should include dataset version and source status.
