# Future MCP Support

Expose `dynamic_pricing` as `opencommerce.dynamic_pricing`.

The MCP tool should return recommendations only and should never execute price changes without a separate authorized action tool.
