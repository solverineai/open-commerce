# Future MCP Support

Expose `inventory_health_check` as `opencommerce.inventory_health_check`.

The MCP tool should accept `inventory.schema.json` compatible records and return advisory risk fields.
